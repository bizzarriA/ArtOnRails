<p id="notice"><%= notice %></p>
<%if current_user != nil && current_user.id == @user.id%>
  <h2>Il Tuo Profilo!</h2>
<%else if @user.artista_id != nil %>
    <h2>Profilo Artista!</h2>
  <%else%>
    <h2>Profilo Utente!</h2>
  <%end%>
<%end%>

<div >
  <p class="col-12 col-12-narrower">
    <%= @user.nome%>
  </p>
  <p class="col-12 col-12-narrower">
    <%= @user.cognome%>
  </p>
  <p class="col-12 col-12-narrower">
    <%= @user.email%>
  </p>
  <% if current_user != nil && current_user.id == @user.id %>
    <p>
      <%= link_to "i miei acquisti", acquistos_path%>
    </p>
    <%if @user.artista_id != nil %>
      <p>
        <%= link_to "Le mie Aste!", artistum_aste_path(@user.artista_id)%>
      </p>
    <%end %>
    <p>
      <%= link_to "Le mie Offerte!", user_offerte_path%>
    </p>
    <p>
      <%= link_to "metodi di pagamento", pagamentos_path%>
    </p>
  <%end %>
</div>


<%if @user.artista_id != nil %>
  <%= link_to 'Portfolio', artistum_path(@user.artista_id) %> |
<%end %>
<% if current_user != nil %>
  <% if current_user.id == @user.id %>
    <%= link_to 'Edit', edit_user_registration_path(@user) %> |
  <%end %>
<%end %>
<%= link_to 'Esci', user_sign_out_path %>
